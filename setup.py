from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Classify digits from images using the MNIST dataset',
    author='Jeffrey Nirschl',
    license='MIT',
)
